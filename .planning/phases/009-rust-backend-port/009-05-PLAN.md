---
phase: 009-rust-backend-port
plan: 05
type: execute
wave: 4
depends_on: [09-04]
files_modified:
  - src/chilmesh_core/lib.rs
  - tests/test_skeletonization_rust.py
autonomous: true
requirements: [F-003]
must_haves:
  truths:
    - "Skeletonization layer counts match Python reference ±1 layer on all 4 fixtures"
    - "All 439 tests PASS via Python wrapper (no test modifications needed)"
    - "Layer coverage invariant satisfied: sum(oe + ie per layer) = total n_elems"
    - "OE/IE/OV/IV classification correct: no orphaned vertices or elements"
  artifacts:
    - path: "tests/test_skeletonization_rust.py"
      provides: "Skeletonization equivalence tests and invariant validation"
      contains: "def test_skeleton_"
---

<objective>
Complete skeletonization validation and integrate with Python test suite. Ensure all 439 existing tests pass via Rust backend.

**Purpose:** Validate skeletonization algorithm and confirm Phase 4 correctness before moving to I/O writer (Wave 5).

**Output:** All skeletonization tests passing; Python test suite fully integrated with Rust backend.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PHASE_9_RUST_PORT_PLAN.md
- `tests/test_skeletonization.py` — Phase 008 tests (reference)
- `tests/conftest.py` — Test fixtures and parametrization
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skeletonization equivalence tests and invariant validation</name>
  <files>tests/test_skeletonization_rust.py</files>
  <read_first>
    - Review existing Phase 008 tests: `tests/test_skeletonization.py` (if exists) or search in test suite
    - Understand test fixture loading from conftest.py (annulus, donut, block_o, structured)
  </read_first>
  <action>
Create `tests/test_skeletonization_rust.py` with:

**Test 1: test_skeleton_layer_count**
- For each fixture:
  - Load via Rust: mesh = RustMesh.read_from_fort14(fixture_path)
  - Call mesh.skeletonize()
  - Compare layer count with Python reference ±1 layer
  - Assert: layer_count > 0

**Test 2: test_skeleton_coverage_invariant**
- For each fixture:
  - Load and skeletonize
  - Sum elements across all layers: sum(oe + ie)
  - Assert: sum == n_elems (coverage invariant)

**Test 3: test_skeleton_oe_ie_classification**
- For annulus (simplest fixture):
  - Load and skeletonize
  - For each layer:
    - Assert: len(oe) > 0 and len(ie) >= 0
    - Assert: oe ∩ ie = ∅ (disjoint)
    - Assert: each OE has ≥1 boundary edge
    - Assert: each IE has 0 boundary edges

**Test 4: test_skeleton_vertex_classification**
- For annulus:
  - For each layer:
    - Assert: len(ov) > 0 (outer verts)
    - Assert: ov ∩ iv = ∅ (disjoint)
    - Assert: ov ⊂ vertices incident to OE

**Test 5: test_skeleton_quality_pruning** (optional)
- If quality_threshold set:
  - Assert: no layer below threshold remains
  - Assert: sum elements still = total

Parametrize all tests over 4 fixtures where applicable.
  </action>
  <verify>
    <automated>cd /home/user/CHILmesh && python -m pytest tests/test_skeletonization_rust.py -v 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test_skeletonization_rust.py exists: [ -f tests/test_skeletonization_rust.py ]
    - Test functions defined: grep -c "^def test_skeleton" tests/test_skeletonization_rust.py >= 4
    - Invariant assertions present: grep "assert\|coverage\|invariant" tests/test_skeletonization_rust.py | wc -l >= 10
  </acceptance_criteria>
  <done>Skeletonization tests created with invariant validation</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Python test suite with Rust backend (conftest.py update)</name>
  <files>tests/conftest.py</files>
  <read_first>
    - Read conftest.py to understand current fixture loading and parametrization
    - Identify where topology_backend is set or can be set
  </read_first>
  <action>
Update `tests/conftest.py` to support optional Rust backend:

1. Add environment variable check at top:
   ```python
   import os
   CHILMESH_TOPOLOGY_BACKEND = os.getenv('CHILMESH_TOPOLOGY_BACKEND', 'python')
   ```

2. In fixture loading (e.g., annulus fixture):
   - If CHILMESH_TOPOLOGY_BACKEND == 'rust':
     - Load via Rust: `from chilmesh_core import RustMesh; mesh_obj = RustMesh.read_from_fort14(...)`
   - Else:
     - Load via Python: `from chilmesh import CHILmesh; mesh_obj = CHILmesh.read_from_fort14(...)`
   - Return mesh_obj

3. Alternative approach (no conftest changes):
   - Existing tests call `CHILmesh.read_from_fort14()`
   - After Python wrapper complete (Wave 2), Python CHILmesh delegates to Rust automatically
   - Set CHILMESH_TOPOLOGY_BACKEND=rust to force Rust backend

**Strategy:** Make backend selection environment-driven, allowing single test suite to run against either Python or Rust implementation without code modifications.

Rationale: Per SPEC.md acceptance "All 439 tests PASS unmodified." This requires Python API to remain unchanged; wrapper delegates to Rust transparently. conftest updates are internal testing infrastructure, not user-facing API changes.
  </action>
  <verify>
    <automated>cd /home/user/CHILmesh && CHILMESH_TOPOLOGY_BACKEND=rust python -m pytest tests/test_skeletonization_rust.py::test_skeleton_coverage_invariant -v 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - conftest.py updated: grep "CHILMESH_TOPOLOGY_BACKEND\|topology_backend" tests/conftest.py | wc -l >= 2
    - Environment variable respected: CHILMESH_TOPOLOGY_BACKEND=rust python -c "import os; print(os.getenv('CHILMESH_TOPOLOGY_BACKEND'))" outputs "rust"
  </acceptance_criteria>
  <done>conftest.py updated to support Rust backend via environment variable</done>
</task>

<task type="auto">
  <name>Task 3: Run full Python test suite against Rust backend and validate equivalence</name>
  <files>tests/test_rust_integration.py</files>
  <read_first>
    - Identify all pytest test files: `find tests -name "test_*.py" -type f | grep -v __pycache__`
    - Note which tests are heavy (block_o) vs fast (annulus)
  </read_first>
  <action>
Create `tests/test_rust_integration.py` with integration tests:

**Test: test_all_439_tests_via_rust_backend**
- Run all pytest tests with CHILMESH_TOPOLOGY_BACKEND=rust
- Assert: 439/439 PASS (or count actual passing tests)
- Document: which tests pass, which fail (if any)

**Test: test_rust_equivalence_audit**
- For each of 4 fixtures:
  - Load via Python backend: py_mesh = CHILmesh.read_from_fort14(fixture, backend='python')
  - Load via Rust backend: ru_mesh = RustMesh.read_from_fort14(fixture)
  - Compare outputs:
    - Adjacencies (edge2vert, elem2edge, etc.) — bit-identical or numerical tolerance
    - Skeletonization (layer counts, element counts per layer) — exact match or ±1
    - Quality metrics (signed_area) — ±0.1% tolerance
  - Assert: all comparisons pass

**Rationale:** SPEC.md requirement "All 439 tests PASS via Python wrapper" requires integration test. Equivalence audit ensures Rust output matches Python reference within acceptable tolerances.
  </action>
  <verify>
    <automated>cd /home/user/CHILmesh && python -m pytest tests/test_rust_integration.py::test_all_439_tests_via_rust_backend -v 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test_rust_integration.py exists: [ -f tests/test_rust_integration.py ]
    - Integration test function present: grep -c "def test_all_439_tests_via_rust_backend\|def test_rust_equivalence_audit" tests/test_rust_integration.py >= 2
  </acceptance_criteria>
  <done>Integration tests created; Python test suite ready to run against Rust backend</done>
</task>

</tasks>

<verification>
At end of Wave 4 (Plans 04–05):

1. **Skeletonization algorithm implemented:** build succeeds
2. **Invariant validation:** coverage, OE/IE/OV/IV classification correct
3. **Equivalence tests passing:** skeletonization output matches Python ±1 layer on all fixtures
4. **Python test suite integrated:** conftest.py supports Rust backend via env var
5. **All 439 tests ready to pass:** integration test infrastructure in place

**Go/No-Go for Wave 5:** If skeletonization tests pass and invariants validated, proceed to I/O writer + mutation prep. If failures, debug quad-edge + skeletonization logic against Python reference.
</verification>

<success_criteria>
- ✅ Skeletonization layer extraction algorithm complete
- ✅ OE/IE/OV/IV classification working correctly
- ✅ Layer invariants validated (coverage, disjoint sets, completeness)
- ✅ Equivalence tests passing (layer counts ±1, element distributions match)
- ✅ Python test suite integrated with Rust backend
- ✅ All 439 tests ready to pass (infrastructure in place)

**Effort:** ~42 hours total (Waves 4–5 combined)
**Risk:** Medium (algorithm correctness high, but equivalence tests mitigate)
</success_criteria>

<output>
Create `.planning/phases/009-rust-backend-port/009-05-SUMMARY.md` when done
</output>
