---
phase: 009-rust-backend-port
plan: 08
type: execute
wave: 7-8
depends_on: [09-07]
files_modified:
  - src/chilmesh_core/lib.rs
  - src/chilmesh_core/adjacency.rs
  - src/chilmesh_core/skeletonization.rs
  - docs/RUST_PERFORMANCE.md
  - README.md
  - src/chilmesh/__init__.py
autonomous: true
requirements: [N-001, N-002, N-003, N-004, N-005]
must_haves:
  truths:
    - "Performance: Full init ≤ 3.5s WNAT_Hagen (1.1× EdgeMap baseline) or documented reason for variance"
    - "Memory: Peak usage ≤ 25% vs EdgeMap baseline"
    - "Compile time: debug < 60s, release < 120s"
    - "CI/CD: GitHub Actions matrix passes on Linux + macOS + Windows"
    - "Documentation: Performance characteristics, migration guide, API stable"
  artifacts:
    - path: "docs/RUST_PERFORMANCE.md"
      provides: "Performance analysis and optimization notes"
      contains: "Full init timing"
    - path: "README.md"
      provides: "Updated with Rust backend note"
      contains: "Phase 009"
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions matrix for multi-platform builds"
      exports: ["linux", "macos", "windows"]
  key_links:
    - from: "Performance measurements"
      to: "PHASE_9_RUST_PORT_PLAN.md Wave 7"
      via: "benchmark_quadegg_variants.py extended"
      pattern: "full_init"
---

<objective>
Optimize Rust implementation if performance below target; profile bottlenecks; document performance characteristics; prepare for release. Complete CI/CD and documentation.

**Purpose:** Ensure production readiness (performance validated, builds reliable on all platforms, documented).

**Output:** Performance targets met or waived with justification; CI/CD passing; documentation complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PHASE_9_RUST_PORT_PLAN.md — Waves 7–8 optimization and release prep
@.planning/phases/009-rust-backend-port/009-RESEARCH.md — Performance literature and profiling tools
- `scripts/benchmark_quadegg_variants.py` — Extended for Rust measurement
- `.github/workflows/ci.yml` — Existing CI setup (to be updated for Rust builds)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure performance on all 4 fixtures; profile if > 1.2× target</name>
  <files>scripts/benchmark_rust.py</files>
  <read_first>
    - Review benchmark_quadegg_variants.py structure (fixture loading, timing, reporting)
    - Understand SPEC.md performance target: ≤ 3.5s WNAT_Hagen (full_init)
  </read_first>
  <action>
Create `scripts/benchmark_rust.py` (extension of benchmark_quadegg_variants.py):

**Benchmarking operations:**
1. fast_init (parse + CCW check, no adjacency)
   - Target: < 100ms
2. full_init (parse + adjacency + skeletonization)
   - Target: ≤ 3.5s WNAT_Hagen
3. quality_analysis (signed_area computation)
   - Target: < 100ms
4. query_latency (get_vertex_edges for random vertices)
   - Target: < 10µs per query (amortized)

**Measurement protocol:**
- Run 3 trials per operation per fixture (to measure variance)
- Exclude block_o from default run (too slow for routine builds); run separately if time permits
- Report mean ± std dev
- Compare vs EdgeMap baseline (from Phase 008 benchmark results)

**Performance gates:**
- If full_init_WNAT_Hagen ≤ 3.5s: ✅ PASS
- If 3.5s < full_init ≤ 4.2s (1.2× target): Flag for optimization (Wave 7)
- If full_init > 4.2s: Trigger profiling (cargo flamegraph)

**Profiling (if triggered):**
- `cargo flamegraph --release` on hot path (skeletonization or adjacency)
- Identify top 3 bottlenecks (allocation, loop overhead, memory access)
- Document findings in RUST_PERFORMANCE.md

Rationale: SPEC.md N-001 requires full_init ≤ 3.5s. Benchmarking validates or triggers optimization. Profiling identifies low-hanging fruit (e.g., unnecessary Vec allocations in loops).
  </action>
  <verify>
    <automated>cd /home/user/CHILmesh && python scripts/benchmark_rust.py 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - benchmark_rust.py exists: [ -f scripts/benchmark_rust.py ]
    - Measurement function for all 4 operations: grep -c "def.*fast_init\|def.*full_init\|def.*quality\|def.*query" scripts/benchmark_rust.py >= 1
    - Fixture parametrization: grep "for fixture in.*annulus\|parametrize" scripts/benchmark_rust.py | wc -l >= 1
    - Reporting: grep "mean\|std\|variance" scripts/benchmark_rust.py | wc -l >= 2
  </acceptance_criteria>
  <done>Benchmark script created; performance measured and reported</done>
</task>

<task type="auto">
  <name>Task 2: Document performance characteristics and optimization notes</name>
  <files>docs/RUST_PERFORMANCE.md</files>
  <read_first>
    - Review benchmark results from Task 1
    - Check profiling output (if any bottlenecks identified)
  </read_first>
  <action>
Create `docs/RUST_PERFORMANCE.md`:

**Section 1: Benchmark Results**
- Full init timings on WNAT_Hagen (vs EdgeMap baseline)
- Other operations (query, quality, skeletonization per-layer)
- Variance across 3 trials (< 5% acceptable)

**Section 2: Memory Profile**
- Peak memory usage on block_o mesh (vs EdgeMap)
- Allow ≤ 25% increase per SPEC.md

**Section 3: Optimization Decisions**
- If performance ≤ 3.5s (no optimization needed):
  - Document: "Rust implementation meets performance target without additional optimization. Compiled with LTO + opt-level=3."
- If performance 3.5–4.2s (optimization possible):
  - Document bottleneck identified: "Skeletonization layer extraction allocates Vec per layer; minor overhead acceptable for correctness"
  - Optimization deferred to Phase 9.1 if needed
- If performance > 4.2s (requires investigation):
  - Document: "Bottleneck: [specific function]. Recommendation: [optimization strategy]."
  - Profiling results (flamegraph)

**Section 4: Compilation Time**
- Cargo build --release time on clean build
- Expected: < 120s per SPEC.md N-004

**Section 5: Binary Size**
- Compiled .so/.pyd size (unstripped)
- Expected: < 50 MB per SPEC.md N-005

**Section 6: Conclusions**
- "Phase 009 Rust backend achieves X% of performance target. [Ready for production / Optimization recommended] per SPEC.md criteria."

Rationale: SPEC.md requires documentation of performance characteristics (N-001 to N-005). This file provides transparency and justification for any deviations.
  </action>
  <verify>
    <automated>cd /home/user/CHILmesh && [ -f docs/RUST_PERFORMANCE.md ] && wc -l docs/RUST_PERFORMANCE.md | awk '{print $1}'</automated>
  </verify>
  <acceptance_criteria>
    - docs/RUST_PERFORMANCE.md exists: [ -f docs/RUST_PERFORMANCE.md ]
    - Minimum content: wc -l docs/RUST_PERFORMANCE.md >= 20
    - Benchmark results section: grep -c "Benchmark\|full init\|WNAT" docs/RUST_PERFORMANCE.md >= 2
    - Memory profile section: grep -c "Memory\|peak\|MB" docs/RUST_PERFORMANCE.md >= 2
    - Compilation time: grep -c "Compilation\|cargo build" docs/RUST_PERFORMANCE.md >= 1
  </acceptance_criteria>
  <done>RUST_PERFORMANCE.md created with benchmark results, memory profile, and optimization notes</done>
</task>

<task type="auto">
  <name>Task 3: Update README and documentation; prepare CI/CD for multi-platform builds</name>
  <files>README.md, .github/workflows/ci.yml, src/chilmesh/__init__.py</files>
  <read_first>
    - Review current README.md structure (if exists)
    - Check existing CI setup (.github/workflows/ci.yml)
    - Understand maturin build process for multi-platform wheels
  </read_first>
  <action>
**Part A: README.md updates**

Add section after introduction:
```markdown
## Backend Performance (Phase 009)

CHILmesh core is now implemented in Rust for improved performance:
- Full initialization: ≤ 3.5s on WNAT_Hagen (1.1× EdgeMap baseline)
- Backward compatible: All 439 existing tests PASS unmodified
- Python API unchanged: Drop-in replacement for v0.2.0

See [RUST_PERFORMANCE.md](docs/RUST_PERFORMANCE.md) for detailed benchmarks.
```

**Part B: .github/workflows/ci.yml updates**

Add Rust build job (in addition to Python tests):
```yaml
jobs:
  rust-build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v3
      - uses: dtolnay/rust-toolchain@stable
      - run: rustc --version
      - run: cargo build -C src/chilmesh_core --release
      - run: cargo test -C src/chilmesh_core --release (if unit tests exist)
```

Add Python wrapper build job:
```yaml
  pyo3-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: dtolnay/rust-toolchain@stable
      - run: pip install maturin
      - run: maturin develop -C src/chilmesh_core
      - run: python -c "from chilmesh_core import RustMesh; print('OK')"
```

Add integration test job (after builds pass):
```yaml
  integration:
    needs: [rust-build, pyo3-build]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: pip install -e .
      - run: CHILMESH_TOPOLOGY_BACKEND=rust pytest tests/ -v
```

**Part C: src/chilmesh/__init__.py updates**

Update version/docstring:
```python
"""CHILmesh v0.3.0 — Rust-powered mesh topology engine."""

__version__ = "0.3.0"
__backend__ = "rust"  # Indicate Rust backend is active
```

Rationale: Documentation + CI/CD ensures transparency and reliable builds on all platforms. Multi-platform testing catches OS-specific issues (e.g., Windows .pyd vs Linux .so).
  </action>
  <verify>
    <automated>cd /home/user/CHILmesh && grep -q "Phase 009\|Rust" README.md && echo "OK" || echo "MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - README.md updated: grep -c "Phase 009\|Rust\|RUST_PERFORMANCE" README.md >= 2
    - CI/CD updated: [ -f .github/workflows/ci.yml ] && grep -c "rust-build\|maturin\|Rust" .github/workflows/ci.yml >= 2
    - __init__.py updated: grep -c "__backend__\|0.3.0" src/chilmesh/__init__.py >= 1
  </acceptance_criteria>
  <done>README updated; CI/CD configured for multi-platform Rust builds; version bumped to 0.3.0</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-009-12 | Repudiation | Performance claims | Mitigate | Document actual measurements, variance, and methodology in RUST_PERFORMANCE.md. Include benchmark code and reproducibility notes. |
| T-009-13 | Supply Chain | Compiled binaries (CI) | Mitigate | Use official Rust toolchain (dtolnay/rust-toolchain action). Pin versions in Cargo.lock. Verify build logs for integrity. |
</threat_model>

<success_criteria>
- ✅ Performance measured: full_init documented (≤3.5s or flagged if > 1.2× target)
- ✅ Memory profile measured: ≤ 25% vs EdgeMap baseline
- ✅ Compilation time documented: < 120s release build
- ✅ Binary size documented: < 50 MB
- ✅ README updated with Phase 009 info
- ✅ CI/CD configured and passing on Linux + macOS + Windows
- ✅ RUST_PERFORMANCE.md complete with benchmark results and optimization notes

**Effort:** ~30 hours (matches Waves 7–8 estimate)
**Risk:** Low (profiling and documentation; no algorithm changes)
</success_criteria>

<output>
Create `.planning/phases/009-rust-backend-port/009-08-SUMMARY.md` when done. Summarize:
- Performance: full_init time on WNAT_Hagen (vs 3.5s target)
- Memory: peak usage (vs 25% increase allowed)
- CI/CD: all platforms green
- Documentation: RUST_PERFORMANCE.md, README updated
- Next steps: Phase 009 complete; ready for Phase 010 (k-d tree spatial indexing, mesh mutation Phase 2)
</output>
