---
phase: 009-rust-backend-port
plan: 07
type: execute
wave: 6
depends_on: [09-06]
files_modified:
  - tests/conftest.py
  - tests/test_rust_full_integration.py
  - tests/test_rust_equivalence_audit.py
autonomous: false
requirements: [F-001, F-002, F-003, F-004, F-005, F-006, F-008, N-001, N-002, N-003]
must_haves:
  truths:
    - "All 439 pytest tests PASS with Rust backend (CHILMESH_TOPOLOGY_BACKEND=rust python -m pytest)"
    - "Adjacency outputs bit-identical to Python reference on all 4 fixtures (canonical form)"
    - "Skeletonization layer counts within ±1 of Python reference"
    - "Performance: Full initialization ≤ 3.5s WNAT_Hagen (1.1× EdgeMap baseline)"
    - "Memory usage ≤ 25% increase vs EdgeMap baseline"
    - "Performance variance < 5% across 3 trials per operation"
  artifacts:
    - path: "tests/test_rust_full_integration.py"
      provides: "Full 439-test integration via Python API"
      contains: "def test_all_"
    - path: "tests/test_rust_equivalence_audit.py"
      provides: "Bit-identity checks vs Python reference"
      contains: "def test_equivalence_"
  key_links:
    - from: "Python test suite"
      to: "Rust backend (via CHILmesh wrapper)"
      via: "PyO3 FFI"
      pattern: "_rust_mesh"
---

<objective>
Integrate full Python test suite with Rust backend; run all 439 tests; validate performance targets; conduct equivalence audit. This is the critical validation gate — confirms Phase 009 readiness for production.

**Purpose:** Prove Rust backend is production-ready (correct + fast + compatible).

**Output:** All 439 tests passing; equivalence audit complete; performance targets met or documented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PHASE_9_RUST_PORT_PLAN.md — Wave 6 integration testing section
@.planning/phases/009-rust-backend-port/009-RESEARCH.md — Common Pitfalls section for validation checklist
- `scripts/benchmark_quadegg_variants.py` — Performance measurement framework
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full Python test suite (439 tests) against Rust backend</name>
  <files>tests/test_rust_full_integration.py</files>
  <read_first>
    - Identify all pytest test files: find tests -name "test_*.py" -type f
    - Count total tests: grep -r "^def test_" tests/ | wc -l (should be ~439)
    - Review conftest.py environment variable support from Plan 05
  </read_first>
  <action>
Create `tests/test_rust_full_integration.py`:

**Test: test_all_439_tests_pass_via_rust_backend**

Strategy:
1. Set environment: CHILMESH_TOPOLOGY_BACKEND=rust
2. Import all test modules (conftest loads fixtures with Rust backend)
3. Run pytest with marker: `pytest tests/ -k "not slow" --tb=short` (exclude block_o if too slow)
4. Collect results:
   - Count PASSED tests
   - List any FAILED tests (if any)
   - Document failures with context
5. Assert: PASSED >= 439 (or nearest feasible without block_o)

Alternative: Run via subprocess with explicit env var:
```python
import subprocess
result = subprocess.run(
    ['python', '-m', 'pytest', 'tests/', '-v'],
    env={**os.environ, 'CHILMESH_TOPOLOGY_BACKEND': 'rust'},
    capture_output=True,
    text=True
)
assert 'failed' not in result.stdout.lower() or result.returncode == 0
```

**Test: test_all_fixtures_loadable**
- For each fixture (annulus, donut, block_o, structured):
  - Load via Python wrapper: mesh = CHILmesh.read_from_fort14(fixture_path)
  - Assert: mesh.n_verts > 0, mesh.n_elems > 0
  - Assert: mesh.adjacencies keys correct (edge2vert, elem2edge, etc.)
  - Assert: no exceptions raised

**Note:** Checkpoint placeholder — human will review test results and confirm all 439 pass.
  </action>
  <verify>
    <automated>cd /home/user/CHILmesh && CHILMESH_TOPOLOGY_BACKEND=rust python -m pytest tests/ -q --tb=no 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - test_rust_full_integration.py exists: [ -f tests/test_rust_full_integration.py ]
    - Test function present: grep -c "def test_all_439_tests_pass" tests/test_rust_full_integration.py == 1
    - Subprocess invocation: grep "subprocess.run\|pytest" tests/test_rust_full_integration.py | wc -l >= 1
  </acceptance_criteria>
  <done>Full integration test scaffolding created; ready for human verification</done>
</task>

<task type="auto">
  <name>Task 2: Conduct equivalence audit (Rust vs Python reference on all 4 fixtures)</name>
  <files>tests/test_rust_equivalence_audit.py</files>
  <read_first>
    - Review expected outputs from Phase 008 quad-edge tests
    - Understand canonical form (sorted edge lists, consistent ordering)
  </read_first>
  <action>
Create `tests/test_rust_equivalence_audit.py`:

**Test: test_adjacency_bit_identical**
- For each fixture (annulus, donut, block_o, structured):
  - Load via Python: py_mesh = CHILmesh.read_from_fort14(fixture, backend='python') [if backend selection available]
  - Load via Rust: ru_mesh = CHILmesh.read_from_fort14(fixture)  [Rust is now default]
  - Compare adjacencies:
    - py_edge2vert == ru_edge2vert (assert arrays equal)
    - py_elem2edge == ru_elem2edge
    - py_elem2vert == ru_elem2vert
  - Assert: all equal → "bit-identical adjacency"

**Test: test_quality_metrics_tolerance**
- For each fixture:
  - Load and compute quality (signed_area)
  - Compare signed_area arrays:
    - Allow tolerance: ±0.1% per element or ±1e-6 absolute
    - Assert: np.allclose(py_areas, ru_areas, rtol=0.001, atol=1e-6)

**Test: test_skeletonization_equivalence**
- For each fixture:
  - Run skeletonization
  - Compare layer counts: abs(py_layer_count - ru_layer_count) <= 1
  - For each layer:
    - Compare len(oe), len(ie), len(ov), len(iv)
    - Allow exact match or ±1 element per class (accounting for tied elements)

**Test: test_fort14_roundtrip**
- Load fixture → write to temp file → load temp file
- Compare original vs roundtrip:
  - n_verts, n_elems match
  - points match ±1e-8 (floating-point precision)
  - connectivity matches

All tests parametrized over 4 fixtures. Failures documented with context (which fixture, which values differ).
  </action>
  <verify>
    <automated>cd /home/user/CHILmesh && python -m pytest tests/test_rust_equivalence_audit.py -v 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test_rust_equivalence_audit.py exists: [ -f tests/test_rust_equivalence_audit.py ]
    - Test functions present: grep -c "def test_" tests/test_rust_equivalence_audit.py >= 4
    - Parametrization: grep "pytest.mark.parametrize" tests/test_rust_equivalence_audit.py | wc -l >= 1
    - Tolerance handling: grep "allclose\|rtol\|atol" tests/test_rust_equivalence_audit.py | wc -l >= 2
  </acceptance_criteria>
  <done>Equivalence audit tests created; ready for validation</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>
Full Python test suite integrated with Rust backend (439 tests). Equivalence audit framework prepared to compare Rust outputs vs Python reference.
  </what-built>
  <how-to-verify>
1. Run full integration: `cd /home/user/CHILmesh && CHILMESH_TOPOLOGY_BACKEND=rust python -m pytest tests/ -v 2>&1 | tail -20`
   - Expected: "passed" count = 439 (or close to it, excluding skipped tests)
   - If failures: document which tests fail and why

2. Run equivalence audit: `cd /home/user/CHILmesh && python -m pytest tests/test_rust_equivalence_audit.py -v 2>&1`
   - Expected: all tests PASS (adjacency bit-identical, quality ±0.1%, skeletonization ±1 layer)
   - If failures: compare specific outputs and determine if acceptable tolerance or real bug

3. Performance check (if time permits):
   - `python scripts/benchmark_quadegg_variants.py` (extended for Rust backend)
   - Expected: full_init on WNAT_Hagen ≤ 3.5s
   - Document actual time vs target

4. Memory baseline:
   - Load block_o mesh and measure peak memory
   - Expected: ≤ 25% increase vs EdgeMap baseline
  </how-to-verify>
  <resume-signal>
Type "approved" to confirm all tests passing and equivalence audit valid. Or describe any failures found. Include:
- Total test pass count
- Any test failures (with error messages)
- Performance time (if measured)
- Memory usage (if measured)
  </resume-signal>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-009-10 | Logic Error | Test equivalence tolerance | Mitigate | Document tolerance rationale: bit-identical for adjacency (integer arrays), ±0.1% for quality (floating-point), ±1 layer for skeletonization (discretization). Justify each tolerance in test comments. |
| T-009-11 | Information Disclosure | Test output | Mitigate | Exclude PII/internals from test reports; only document pass/fail counts and summary stats. |
</threat_model>

<success_criteria>
- ✅ All 439 tests PASS via Python wrapper (Rust backend)
- ✅ Equivalence audit passes (adjacency bit-identical, quality ±0.1%, skeleton ±1)
- ✅ Performance ≤ 3.5s WNAT_Hagen documented
- ✅ Memory usage ≤ 25% increase baseline documented
- ✅ Variance < 5% across 3 trials (if measured)

**Effort:** ~18 hours (matches Wave 6 estimate)
**Risk:** Medium (test framework; pass/fail not under planner control) → Checkpoint for human verification
</success_criteria>

<output>
Create `.planning/phases/009-rust-backend-port/009-07-SUMMARY.md` after human verifies test results
</output>
