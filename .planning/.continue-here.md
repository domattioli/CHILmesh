---
context: default
phase: post-phase-5
status: complete
last_updated: 2026-05-24T03:11:49Z
tracking: GitHub issues (no GSD phase dirs)
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Discovered through actual failure this session. Acknowledge before proceeding.

- [ ] CONSTRAINT: **Editable install points at `/workspace/CHILmesh`, NOT `/home/user/CHILmesh`** — Edits to `/home/user/CHILmesh/src` are invisible to `pytest`/`import chilmesh` until you run `pip install -e /home/user/CHILmesh`. Cost ~30 min of false test failures this session. **Mitigation: at session start, run `python3 -c "import chilmesh; print(chilmesh.__file__)"` and confirm it resolves under the repo you are editing. Reinstall if not.**

**Do not proceed until checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Stale editable install | Package imported from `/workspace` while edits land in `/home/user`; tests silently run old code | blocking | Verify `chilmesh.__file__` at session start; `pip install -e /home/user/CHILmesh` |
| Edge-ID reorder breaks C++ parity | Changing edge enumeration order (slot-major vs element-major) silently breaks `test_cpp_layer_member_sets_match_python[bEdgeIDs]` because C++ backend uses element-major first-encounter | blocking | Any adjacency rewrite must preserve element-major slot-minor traversal + first-occurrence edge IDs |

<current_state>
Phase 5 epic #94 COMPLETE and merged. PR #165 squash-merged to `main` as `fa50a89`. All 939 tests pass, 19 skipped. v1.2.0. Working branch `daily-issue-fixing` is even with merged work.
</current_state>

<completed_work>

- Phase 5 mutation API (#94): remove_vertex, collapse_edge, move_boundary_node, split_triangles, smooth_topology, _patch_swap_adjacency (incremental O(1) adjacency), reskeletonize_local, skeletonize_diff — Done
- #164 vectorize `_build_adjacencies`: np.unique(axis=0) + first-occurrence argsort; Elem2Edge/Edge2Elem/Vert2Edge/Vert2Elem via scatter+groupby; ~190 lines of Python loops deleted — Done
- #139 optimize `angle_based_smoother`: cache elem-vert lists per iteration, reuse in line search — Done
- Archived stale `.planning/PHASE-5-HALF-EDGE-OPTIMIZATION-SPEC.md` → `_archive_` prefix — Done
- PR #165 merged (squash, fa50a89) — Done
</completed_work>

<remaining_work>

Open issues (no work started):
- #163 Rust + C++ backends blocked from cross-language validation benchmark — aligns with user's #94 patchwork-backends direction
- #155 Benchmarking should detail all chilmesh lifecycle steps
- #129 layers/skeletonization should consider boundary types
- #142 promote mesh-element-validity helper to public API
- #122 CI runtime optimization (xdist safety + macOS profiling)
- #105 Re-integrate FEM smoother work (PR #104)
- #75 Plotting library
- #154 .chil file format design
- #153 sibling project mesh-segmenter
- #152 cross-repo label canon sweep (operator triage)
</remaining_work>

<decisions_made>

- Vectorized `_build_adjacencies`: element-major slot-minor traversal (`conn.ravel()` + `np.roll`) to preserve C++ first-encounter edge IDs
- Edge IDs restored via `np.unique(return_index=True)` + `argsort(first_occur)` — NOT raw lexicographic
- `angle_based_smoother`: caching over full vectorization (variable ring sizes resist clean array ops)
</decisions_made>

<blockers>
- `/handoff` and `/introspect` skills not registered this session; `introspect@DomI` plugin not loaded. Used `gsd-pause-work` instead. Install: `claude plugin install introspect@DomI`.
</blockers>

## Required Reading (in order)
1. `.claude/CLAUDE.md` — branch policy (ONLY `daily-issue-fixing`), DomI sync contract
2. `.planning/HANDOFF.json` — machine-readable state mirror of this file

## Infrastructure State
- Branch: `daily-issue-fixing` (push target; PR #165 already merged to main)
- Editable install: `/workspace/CHILmesh` by default — RE-POINT to `/home/user/CHILmesh` before testing
- C++ backend (`chilmesh_cpp`) IS built/available — equivalence tests run

<context>
Optimization sweep (#164, #139) directly serves user's #94 goal: faster CHILmesh primitives propagate downstream to ADMESH/QuADMesh/MADMESHing. Next natural step is either #163 (Rust/C++ backend patchwork — user explicitly flagged) or propagating v1.2.0 to the downstream consumers.
</context>

<next_action>
Confirm `chilmesh.__file__` resolves under the repo being edited, then pick next issue. Recommend #163 (backend patchwork) per user direction, or coordinate v1.2.0 downstream adoption.
</next_action>
