diff --git a/admesh/__init__.py b/admesh/__init__.py
index f5e7518..f7dcb36 100644
--- a/admesh/__init__.py
+++ b/admesh/__init__.py
@@ -12,6 +12,7 @@ from admesh.api import (
 )
 from admesh.boundary_types import BoundaryType
 from admesh.fort14 import Fort14ParseError, read_fort14, write_fort14
+from admesh.gmsh import GmshParseError, read_msh, write_msh
 from admesh.loaders import (
     load_domain_from_fort14,
     load_domain_from_json,
@@ -56,6 +57,9 @@ __all__ = [
     "Fort14ParseError",
     "read_fort14",
     "write_fort14",
+    "GmshParseError",
+    "read_msh",
+    "write_msh",
     # --- Size-field composition ---
     "SizeFieldFn",
     "compose_size_field",
diff --git a/admesh/api.py b/admesh/api.py
index b593124..c0510ba 100644
--- a/admesh/api.py
+++ b/admesh/api.py
@@ -115,6 +115,17 @@ class Mesh:
 
         write_fort14(self, path)
 
+
+    def to_msh(
+        self,
+        path: "str | os.PathLike[str]",
+        *,
+        version: str = "4.1",
+    ) -> None:
+        """Alias for admesh.write_msh(self, path, version=version)."""
+        from admesh.gmsh import write_msh
+        write_msh(self, path, version=version)
+
     def plot(self, ax=None, **kwargs):
         """Draw the mesh using matplotlib.
 
