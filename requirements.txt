# Runtime dependencies for chilmesh.
# Authoritative source is pyproject.toml; this file is kept for users who
# prefer ``pip install -r requirements.txt`` from a checkout.
numpy>=1.23
scipy>=1.10
matplotlib>=3.6
