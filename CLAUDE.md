# CLAUDE.md

Project memory (a DomI consumer). Claude Code auto-discovers this root file; the full content lives in `.claude/` to keep the repo root clean.

@.claude/CLAUDE.md

> Edit memory at `.claude/CLAUDE.md`. This root file is a loader stub only. Governance (Context Management policy, hard stops) inherited from DomI via `.domi-pin`; see DomI `specs/domi-constitution.md` Article III.
